# Barcode Printer Application Requirements
# Python 3.10+ required

# GUI Framework
flet[all]>=0.80.5
flet-datatable2>=0.80.5

# Barcode Generation
python-barcode[images]>=0.16.1
qrcode[pil]>=8.2

# Image Processing
Pillow>=12.1.1

# Note: pywin32 is NOT required.
# Printer access, clipboard, and GDI printing are handled via ctypes (built-in).